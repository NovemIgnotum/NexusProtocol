name: CI Safety Net

on:
  push:
    branches: [dev]

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      test_status: ${{ steps.tests.outputs.status }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt pytest

      - name: Run tests
        id: tests
        run: |
          if pytest; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failure" >> $GITHUB_OUTPUT
          fi

  handle-failure:
    needs: test
    if: needs.test.outputs.test_status == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Authenticate with token
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/NovemIgnotum/NexusProtocol.git

      - name: Debug remote URL
        run: git remote -v

      - name: Create and push failure branch
        run: |
          FAILURE_BRANCH="failure-$(date +%Y%m%d-%H%M%S)"
          git checkout -b $FAILURE_BRANCH
          git push origin $FAILURE_BRANCH
          echo "Created failure branch: $FAILURE_BRANCH"

      - name: Revert dev branch
        run: |
          git checkout dev
          git reset --hard HEAD~1
          git push origin dev --force
          echo "Dev branch reverted to previous commit"
